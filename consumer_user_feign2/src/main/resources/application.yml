spring:
  application:
    name: ConsumerUserFeign2
server:
  port: 7002
eureka:
  client:
    service-url:
      defaultZone:
        http://localhost:9000/eureka/
feign:
  hystrix:
    enabled: true